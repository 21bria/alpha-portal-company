<template>
    <NuxtLink :to="`/news/${slug}`"
        class="group overflow-hidden rounded-[1.0rem] border border-zinc-200 bg-white transition-all duration-500 hover:-translate-y-1 hover:border-amber-300/40 hover:shadow-[0_0_40px_rgba(245,158,11,0.10)]">
        <!-- image -->
        <div class="relative overflow-hidden">
            <img :src="image" :alt="title"
                class="h-42 w-full object-cover transition-transform duration-700 group-hover:scale-105" />

            <div class="absolute inset-0 bg-gradient-to-t from-black/60 via-black/10 to-transparent" />
        </div>

        <!-- content -->
        <div class="p-4">
            <div class="flex items-center gap-3 text-xs uppercase tracking-[0.25em]">
                <span class="font-semibold text-amber-500">
                    {{ category }}
                </span>

                <span class="text-zinc-400">•</span>

                <span class="text-zinc-500">
                    {{ date }}
                </span>
            </div>

            <h3 class="mt-3 text-xl font-semibold leading-snug text-zinc-900 transition group-hover:text-amber-600">
                {{ title }}
            </h3>

            <p class="mt-2 text-[15px] leading-relaxed text-zinc-600">
                {{ description }}
            </p>

            <div
                class="mt-4 inline-flex items-center gap-2 text-sm font-semibold text-amber-500 transition-all duration-300 group-hover:gap-3">
                Read more
                <span>→</span>
            </div>
        </div>
    </NuxtLink>
</template>

<script setup lang="ts">
defineProps<{
    slug: string
    image: string
    title: string
    description: string
    category: string
    date: string
}>()
</script>